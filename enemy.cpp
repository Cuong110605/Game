#include "enemy.h"
#include "ctime"
#include "cstdlib"
#include "vector"
/*
Enemy::Enemy(int index)
{

    width = 55;
    height = 55;
    x = rand() % (1100 -width) ;
   /* while(x<=150 ||x>=1000)
    {
         x = rand() % 1100 - width;
    }
    y = 0 - height - (index * 100);

}


void Enemy::updateposition(int index)
{
    y ++;

    if ( y > SCREEN_HEIGHT || alive == false)
    {

          x = rand() % (1100-width) ;
          while(x<=150 ||x>=1000)
          {
           x = rand() % 1100 ;
          }
          alive = 1;
          y = 0 - height - (index * 100);
    }
}

*/

